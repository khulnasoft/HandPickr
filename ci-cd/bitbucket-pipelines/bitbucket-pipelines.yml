image: node:16.16.0
pipelines:
  branches:
    feature/*:
      - step:
          name: Install Khulnasoft CLI, Pull Khulnasoft Preview Environment Information, Build Project Artifacts and Deploy Project Artifacts to Khulnasoft
          script:
            - npm install --global vercel
            - vercel pull --yes --environment=preview --token=$VERCEL_TOKEN
            - vercel build --token=$VERCEL_TOKEN
            - vercel deploy --prebuilt  --token=$VERCEL_TOKEN
    main:
      - step:
          name: Install Khulnasoft CLI, Pull Khulnasoft Production Environment Information, Build Project Artifacts and Deploy Project Artifacts to Khulnasoft
          script:
            - npm install --global vercel
            - vercel pull --yes --environment=production --token=$VERCEL_TOKEN
            - vercel build --prod --token=$VERCEL_TOKEN
            - vercel deploy --prebuilt --prod --token=$VERCEL_TOKEN
